build:
    nodes:
        analysis:
            environment:
                php:
                    version: 7.2
            cache:
                disabled: false
                directories:
                    - ~/.composer/cache
            project_setup:
                override: true

tools:
    external_code_coverage: true

checks:
    php:
        code_rating: true
